extern malloc();
extern free();
extern printf();
extern putchar();
extern exit();

/*
   Statements don't need to be in functions,
   you just need to remember to exit from them
   lmao
*/
main(c,v)
{
        printf(*v);
}